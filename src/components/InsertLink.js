import React from 'react'
import ToolbarButton from "./ToolbarButton";

export default ({

}) => {
    return (
        <React.Fragment>
            <ToolbarButton onClick={ () => {} } action="" icon={ <i className="fas fa-link"></i> }/>
        </React.Fragment>
    )
}
